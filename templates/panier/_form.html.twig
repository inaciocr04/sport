<div class="form_global">
    {{ form_start(form) }}
    <div class="flex gape-50 flex_column">
        {{ form_widget(form) }}
        <div>
            <button class="btn">{{ button_label|default('Ajouter') }}</button>
        </div>
    </div>
    {{ form_end(form) }}
</div>
