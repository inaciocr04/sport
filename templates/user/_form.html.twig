{{ form_start(form) }}
    {{ form_row(form.pseudo) }}
    {{ form_row(form.email) }}
    {{ form_row(form.password) }}
<button type="submit">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
