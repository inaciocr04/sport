<div class="form_global">
    {{ form_start(form) }}
    <div class="flex gape-50">
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Ajouter') }}</button>
    </div>
{{ form_end(form) }}
</div>
